seed: 123
device: cpu

paths:
  data_dir: data
  output_dir: outputs
  checkpoint_dir: checkpoints

data:
  n_grid: 32
  viscosity: 0.01
  time_horizon: 0.1
  time_steps: 20
  initial_condition_modes: 4
  train_samples: 32
  val_samples: 8

model:
  modes: 8
  width: 16
  layers: 3

training:
  batch_size: 8
  learning_rate: 0.001
  epochs: 5

smoke:
  n_samples: 8
  n_grid: 32
  time_steps: 10
  modes: 6
  width: 8
  layers: 2
  train_steps: 1
